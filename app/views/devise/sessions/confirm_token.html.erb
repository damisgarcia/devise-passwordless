<h2>Confirm Token</h2>

<%= form_for(@user, url: user_session_path) do |f| %>
  <div class="field">
    <%= f.label :login_token %><br />
    <%= f.text_field :login_token %>
  </div>

  <div class="actions">
    <%= f.submit "Confirm" %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
